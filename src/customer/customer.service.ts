import { Injectable, NotFoundException } from '@nestjs/common';

@Injectable()
export class CustomerService {
  // createCustomer(customer: Express.Multer.File) {
  //   return customer.buffer || 'null';
  //   // throw new NotFoundException('Method not implemented.', 'Not Found2');
  // }
}
